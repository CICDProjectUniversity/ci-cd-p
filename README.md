# CI/CD Практическая работа

Проект демонстрирует настройку непрерывной интеграции и развертывания (CI/CD) с использованием GitHub Actions.

## 📋 Описание проекта

Простой калькулятор на Python с автоматическим тестированием и генерацией документации через CI/CD pipeline.

## 🚀 Возможности

- ✅ Автоматический запуск тестов при каждом push
- ✅ Unit и интеграционные тесты
- ✅ Автоматическая генерация документации
- ✅ Создание Pull Request с обновлённой документацией
- ✅ Поддержка веток `main` и `development`

## 🏗️ Структура проекта

```
.
├── .github/
│   └── workflows/
│       └── ci-cd.yml          # GitHub Actions workflow
├── calculator.py              # Основной модуль калькулятора
├── test_calculator.py         # Unit-тесты
├── requirements.txt           # Зависимости Python
└── README.md                  # Этот файл
```

## 📦 Установка

1. Клонируйте репозиторий:
```bash
git clone <your-repo-url>
cd <your-repo-name>
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

## 🧪 Запуск тестов локально

```bash
# Запуск всех тестов
python -m unittest test_calculator.py -v

# Запуск конкретного теста
python -m unittest test_calculator.TestCalculator.test_add
```

## 🔄 CI/CD Pipeline

### Workflow состоит из двух jobs:

#### 1. **Test** - Запуск тестов
- Выполняется при каждом push в `main` или `development`
- Выполняется при создании Pull Request
- Запускает unit и интеграционные тесты

#### 2. **Docs** - Генерация документации
- Выполняется после успешного прохождения тестов
- Генерирует документацию из docstrings
- Автоматически создаёт Pull Request с обновлённой документацией

## 📝 Рабочий процесс (Workflow)

### Сценарий 1: Разработка новой функции

1. Создайте ветку `development`:
```bash
git checkout -b development
```

2. Внесите изменения в код

3. Коммит и push:
```bash
git add .
git commit -m "feat: добавлена новая функция"
git push origin development
```

4. CI автоматически:
   - Запустит тесты
   - Сгенерирует документацию
   - Создаст PR с документацией

### Сценарий 2: Создание Pull Request

1. Создайте PR из `development` в `main`:
```bash
gh pr create --base main --head development --title "Новая функция"
```

2. CI проверит:
   - Прохождение всех тестов
   - Отсутствие конфликтов

3. После одобрения и слияния:
   - Автоматически обновится документация
   - Создастся новый PR с актуальными docs

### Сценарий 3: Release

1. Создайте тег `release`:
```bash
git checkout main
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0
```

2. Убедитесь, что все тесты прошли успешно

## 🔧 Настройка в GitHub

### Необходимые шаги:

1. **Создайте репозиторий на GitHub**

2. **Включите GitHub Actions**:
   - Settings → Actions → General
   - Разрешите "Read and write permissions" для GITHUB_TOKEN
   - ✅ Включите "Allow GitHub Actions to create and approve pull requests"

3. **Защитите ветку main**:
   - Settings → Branches → Add rule
   - Выберите `main`
   - Включите "Require status checks to pass before merging"
   - Выберите check "test"

4. **Настройте ветки**:
```bash
# Создайте ветку development
git checkout -b development
git push -u origin development

# Вернитесь на main
git checkout main
```

## 📚 Генерация документации

Документация генерируется автоматически с помощью Python скрипта `generate_docs.py`, который:
- Извлекает docstrings из модулей Python
- Создаёт красиво отформатированную Markdown документацию
- Автоматически запускается при каждом коммите через GitHub Actions
- Создаёт Pull Request с обновлённой документацией

Для локального запуска:
```bash
python generate_docs.py
```

## 📊 Примеры использования

### Базовые операции:

```python
from calculator import Calculator

calc = Calculator()

# Сложение
result = calc.add(2, 3)  # 5

# Вычитание
result = calc.subtract(5, 2)  # 3

# Умножение
result = calc.multiply(4, 3)  # 12

# Деление
result = calc.divide(10, 2)  # 5.0
```

## 🐛 Проверка CI/CD

### Как убедиться, что CI работает:

1. **Проверьте статус Actions**:
   - Перейдите во вкладку "Actions" на GitHub
   - Убедитесь, что workflow запускается

2. **Проверьте создание PR**:
   - После коммита в `development` должен создаться PR с документацией
   - Проверьте вкладку "Pull Requests"

3. **Проверьте документацию**:
   - После merge PR проверьте папку `docs/`
   - Убедитесь, что документация обновилась

## 🎯 Критерии выполнения задания

- ✅ Настроена CI система (GitHub Actions)
- ✅ Репозиторий на GitHub создан
- ✅ Workflow автоматически запускается при каждом коммите
- ✅ Файл main.yml в `.github/workflows/` настроен
- ✅ CI отчёт показывает успешное выполнение
- ✅ Unit-тесты добавлены и проходят
- ✅ Ветка development создана для разработки
- ✅ После коммита в development все тесты прошли
- ✅ PR создан для слияния development → main
- ✅ При успешных тестах автоматически создаётся PR с документацией
- ✅ Документация проекта обновляется через CI/CD
- ✅ Автоматическая генерация документации настроена

## 📚 Дополнительно

### Полезные команды Git:

```bash
# Просмотр статуса
git status

# Просмотр истории
git log --oneline --graph

# Создание ветки
git checkout -b feature-name

# Слияние веток
git merge development

# Отправка в удалённый репозиторий
git push origin branch-name
```

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature-ветку (`git checkout -b feature/AmazingFeature`)
3. Commit изменений (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT License

## 👤 Автор

Ваше имя
