# 📊 Схема CI/CD процесса

## Общая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                      GitHub Repository                       │
│                                                              │
│  ┌──────────┐                           ┌──────────┐        │
│  │   main   │◄──────── merge ───────────│development│        │
│  └────┬─────┘                           └─────┬────┘        │
│       │                                        │             │
│       │ push/merge                             │ push        │
│       ▼                                        ▼             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │           GitHub Actions Workflow                   │    │
│  │                  (.github/workflows/ci-cd.yml)       │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ triggers
                            ▼
        ┌───────────────────────────────────────┐
        │       GitHub Actions Runner            │
        │                                        │
        │  ┌──────────────────────────────────┐ │
        │  │     Job 1: Test                  │ │
        │  │  ┌────────────────────────────┐  │ │
        │  │  │ 1. Checkout code           │  │ │
        │  │  │ 2. Setup Python 3.10       │  │ │
        │  │  │ 3. Install dependencies    │  │ │
        │  │  │ 4. Run unit tests          │  │ │
        │  │  │ 5. Run integration tests   │  │ │
        │  │  └────────────────────────────┘  │ │
        │  └──────────────────────────────────┘ │
        │              │                         │
        │              │ if success              │
        │              ▼                         │
        │  ┌──────────────────────────────────┐ │
        │  │     Job 2: Docs                  │ │
        │  │  ┌────────────────────────────┐  │ │
        │  │  │ 1. Checkout code           │  │ │
        │  │  │ 2. Setup Python 3.10       │  │ │
        │  │  │ 3. Install pydoc-markdown  │  │ │
        │  │  │ 4. Generate documentation  │  │ │
        │  │  │ 5. Check for changes       │  │ │
        │  │  │ 6. Create Pull Request     │  │ │
        │  │  └────────────────────────────┘  │ │
        │  └──────────────────────────────────┘ │
        └───────────────────────────────────────┘
                            │
                            │ creates
                            ▼
        ┌───────────────────────────────────────┐
        │    Pull Request with Documentation     │
        │                                        │
        │  Title: 📚 Автоматическое обновление  │
        │  Branch: docs/auto-update-XXX         │
        │  Files: docs/README.md                │
        │                                        │
        │  [Review] → [Merge] → [Close]         │
        └───────────────────────────────────────┘
```

## Детальный workflow

### 1. Триггеры (on)

```yaml
push:
  branches: [ main, development ]
pull_request:
  branches: [ main ]
```

**Когда запускается:**
- При push в ветки `main` или `development`
- При создании Pull Request в ветку `main`

### 2. Job: Test

```
┌─────────────────────────────────────────┐
│           Job: test                      │
├─────────────────────────────────────────┤
│ runs-on: ubuntu-latest                  │
│                                         │
│ Steps:                                  │
│  1. actions/checkout@v4                 │
│     └─> Клонирует код репозитория       │
│                                         │
│  2. actions/setup-python@v5             │
│     └─> Устанавливает Python 3.10       │
│                                         │
│  3. pip install -r requirements.txt     │
│     └─> Устанавливает зависимости       │
│                                         │
│  4. python -m unittest test_*.py        │
│     └─> Запускает unit-тесты            │
│                                         │
│  5. unittest discover                   │
│     └─> Запускает все тесты             │
└─────────────────────────────────────────┘
        │
        │ exit code 0 (success)
        ▼
   Переход к Job: docs
```

### 3. Job: Docs (зависит от test)

```
┌─────────────────────────────────────────┐
│           Job: docs                      │
├─────────────────────────────────────────┤
│ runs-on: ubuntu-latest                  │
│ needs: test                             │
│ if: branch == development OR main       │
│                                         │
│ Steps:                                  │
│  1. actions/checkout@v4                 │
│     └─> Клонирует код с full history    │
│                                         │
│  2. actions/setup-python@v5             │
│     └─> Устанавливает Python 3.10       │
│                                         │
│  3. pip install pydoc-markdown          │
│     └─> Устанавливает генератор docs    │
│                                         │
│  4. pydoc-markdown -m calculator        │
│     └─> Генерирует Markdown из docstrings│
│                                         │
│  5. git add docs/                       │
│     git diff --staged                   │
│     └─> Проверяет наличие изменений     │
│                                         │
│  6. peter-evans/create-pull-request@v6  │
│     └─> Создаёт PR с документацией      │
└─────────────────────────────────────────┘
        │
        │ if changes detected
        ▼
┌─────────────────────────────────────────┐
│      Создан Pull Request                │
│                                         │
│ Title: 📚 Автоматическое обновление     │
│ Branch: docs/auto-update-{run_number}  │
│ Labels: documentation, automated        │
│                                         │
│ Body:                                   │
│ - Дата генерации                        │
│ - SHA коммита                           │
│ - Ветка источника                       │
└─────────────────────────────────────────┘
```

## Пример сценария использования

### Сценарий: Добавление новой функции

```
Developer                    GitHub                    Actions
    │                          │                          │
    │  1. git checkout -b      │                          │
    │     development          │                          │
    ├─────────────────────────►│                          │
    │                          │                          │
    │  2. Edit code            │                          │
    │     Add tests            │                          │
    │                          │                          │
    │  3. git push origin      │                          │
    │     development          │                          │
    ├─────────────────────────►│                          │
    │                          │                          │
    │                          │  4. Trigger workflow     │
    │                          ├─────────────────────────►│
    │                          │                          │
    │                          │  5. Run tests            │
    │                          │◄─────────────────────────┤
    │                          │     ✅ Tests passed      │
    │                          │                          │
    │                          │  6. Generate docs        │
    │                          │◄─────────────────────────┤
    │                          │     📚 Docs updated      │
    │                          │                          │
    │                          │  7. Create PR            │
    │  8. Review PR            │◄─────────────────────────┤
    │◄─────────────────────────┤                          │
    │                          │                          │
    │  9. Approve & Merge      │                          │
    ├─────────────────────────►│                          │
    │                          │                          │
    │ 10. Create PR to main    │                          │
    ├─────────────────────────►│                          │
    │                          │                          │
    │                          │ 11. Trigger workflow     │
    │                          ├─────────────────────────►│
    │                          │                          │
    │                          │ 12. Run tests            │
    │                          │◄─────────────────────────┤
    │                          │     ✅ Tests passed      │
    │                          │                          │
    │ 13. Merge to main        │                          │
    ├─────────────────────────►│                          │
    │                          │                          │
    │                          │ 14. Final docs update    │
    │                          │◄─────────────────────────┤
    │                          │     📚 PR created        │
    │                          │                          │
    │ 15. Review & Merge docs  │                          │
    │◄─────────────────────────┤                          │
    │                          │                          │
    ▼                          ▼                          ▼
   Done                     Updated                   Success
```

## Ключевые компоненты

### 1. Условия запуска (needs, if)

```yaml
needs: test                    # Ждёт завершения job test
if: github.ref == 'refs/heads/development' OR github.ref == 'refs/heads/main'
```

### 2. Проверка изменений

```bash
git diff --staged --quiet
# exit code 0 = нет изменений
# exit code 1 = есть изменения
```

### 3. Создание PR

```yaml
peter-evans/create-pull-request@v6
  - Создаёт ветку docs/auto-update-{number}
  - Коммитит изменения
  - Создаёт Pull Request
  - Удаляет ветку после merge
```

## Преимущества данной архитектуры

✅ **Автоматизация**: Нет ручных действий для тестирования и документации  
✅ **Проверка качества**: Код не попадёт в main без прохождения тестов  
✅ **Актуальность документации**: Документация всегда соответствует коду  
✅ **Прозрачность**: Все изменения проходят через PR review  
✅ **Разделение веток**: development для разработки, main для продакшена  
✅ **История изменений**: Все обновления документации в отдельных PR  

## Возможные улучшения

🔄 Добавить code coverage reporting  
🔄 Добавить линтеры (pylint, flake8)  
🔄 Добавить security scanning  
🔄 Добавить deployment на staging/production  
🔄 Добавить уведомления в Slack/Discord  
🔄 Добавить создание GitHub Release при tag  
