# 📖 Пошаговая инструкция по настройке CI/CD

## Шаг 1: Создание репозитория на GitHub

1. Перейдите на https://github.com
2. Нажмите "New repository" (зелёная кнопка)
3. Заполните:
   - Repository name: `python-ci-cd-project`
   - Description: `CI/CD практическая работа`
   - Выберите "Public"
   - НЕ ставьте галочки на "Add README" и "Add .gitignore"
4. Нажмите "Create repository"

## Шаг 2: Инициализация локального репозитория

Откройте терминал и выполните:

```bash
# Перейдите в директорию с проектом
cd /path/to/project

# Инициализируйте Git
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: добавлен проект с CI/CD"

# Подключите удалённый репозиторий (замените YOUR_USERNAME на ваш username)
git remote add origin https://github.com/YOUR_USERNAME/python-ci-cd-project.git

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

## Шаг 3: Настройка прав доступа для GitHub Actions

1. Перейдите в Settings репозитория
2. Слева выберите "Actions" → "General"
3. Прокрутите вниз до "Workflow permissions"
4. Выберите "Read and write permissions"
5. Поставьте галочку на "Allow GitHub Actions to create and approve pull requests"
6. Нажмите "Save"

## Шаг 4: Создание ветки development

```bash
# Создайте ветку development
git checkout -b development

# Отправьте её на GitHub
git push -u origin development

# Вернитесь на main
git checkout main
```

## Шаг 5: Проверка работы CI

1. Перейдите во вкладку "Actions" на GitHub
2. Вы должны увидеть запущенный workflow "CI/CD Pipeline"
3. Кликните на него и посмотрите логи выполнения

## Шаг 6: Тестирование автоматического создания PR

```bash
# Переключитесь на development
git checkout development

# Внесите изменение в код (например, добавьте функцию)
# Откройте calculator.py и добавьте новую функцию:

echo '
    def power(self, a: float, b: float) -> float:
        """
        Возведение в степень
        
        Args:
            a: основание
            b: показатель степени
            
        Returns:
            a в степени b
        """
        return a ** b
' >> calculator.py

# Коммит и push
git add calculator.py
git commit -m "feat: добавлена функция возведения в степень"
git push origin development
```

Теперь:
1. Перейдите на GitHub во вкладку "Actions"
2. Дождитесь завершения workflow (примерно 1-2 минуты)
3. Перейдите во вкладку "Pull Requests"
4. Вы должны увидеть автоматически созданный PR с документацией!

## Шаг 7: Создание Pull Request для слияния

```bash
# Через GitHub CLI (если установлен)
gh pr create --base main --head development --title "Добавлена функция power" --body "Добавлена новая функция возведения в степень"

# Или через веб-интерфейс GitHub:
# 1. Перейдите во вкладку "Pull Requests"
# 2. Нажмите "New pull request"
# 3. Выберите base: main, compare: development
# 4. Нажмите "Create pull request"
```

## Шаг 8: Слияние Pull Request

1. Откройте созданный PR
2. Убедитесь, что все проверки (checks) прошли успешно ✅
3. Нажмите "Merge pull request"
4. Нажмите "Confirm merge"
5. После слияния будет создан ещё один PR с обновлённой документацией!

## Шаг 9: Проверка документации

1. Перейдите во вкладку "Pull Requests"
2. Откройте PR с названием "📚 Автоматическое обновление документации"
3. Просмотрите изменения в файлах документации
4. Если всё хорошо, сделайте merge

## Шаг 10: Создание Release (опционально)

```bash
# Переключитесь на main
git checkout main

# Получите последние изменения
git pull origin main

# Создайте тег
git tag -a v1.0.0 -m "Release version 1.0.0"

# Отправьте тег на GitHub
git push origin v1.0.0
```

На GitHub:
1. Перейдите во вкладку "Releases"
2. Нажмите "Create a new release"
3. Выберите тег v1.0.0
4. Заполните описание релиза
5. Нажмите "Publish release"

## ✅ Проверка выполнения всех требований

После выполнения всех шагов проверьте:

- [ ] Репозиторий создан на GitHub
- [ ] Есть ветка main с основным кодом
- [ ] Есть ветка development для разработки
- [ ] Файл `.github/workflows/ci-cd.yml` настроен
- [ ] При push в любую ветку запускается CI
- [ ] Тесты выполняются автоматически
- [ ] После коммита в development/main создаётся PR с документацией
- [ ] Документация генерируется автоматически
- [ ] PR можно просмотреть и смёржить
- [ ] В Actions виден отчёт о выполнении CI

## 🎯 Результат

После выполнения всех шагов у вас будет:

1. **Работающий CI/CD pipeline** с автоматическим тестированием
2. **Автоматическая генерация документации** при каждом изменении кода
3. **Pull Request workflow** для проверки документации
4. **Защищённая ветка main** с обязательными проверками
5. **Ветка development** для разработки новых функций

## 🆘 Решение проблем

### Проблема: Actions не запускаются

**Решение:**
1. Settings → Actions → General
2. Убедитесь, что Actions включены
3. Проверьте "Workflow permissions" (должны быть Read and write)

### Проблема: PR с документацией не создаётся

**Решение:**
1. Проверьте логи в Actions
2. Убедитесь, что "Allow GitHub Actions to create and approve pull requests" включено
3. Проверьте, что есть изменения в документации

### Проблема: Тесты падают

**Решение:**
1. Запустите тесты локально: `python -m unittest test_calculator.py -v`
2. Проверьте логи в Actions
3. Исправьте ошибки в коде

## 📝 Что сдавать преподавателю

1. **Ссылка на репозиторий GitHub**
2. **Скриншот успешного выполнения CI** (вкладка Actions)
3. **Скриншот созданного PR с документацией**
4. **Ссылка на merged PR** (если требуется)
5. **Файл с отчётом**, содержащий:
   - Описание настроенного CI/CD процесса
   - Скриншоты выполнения каждого этапа
   - Вывод о проделанной работе
